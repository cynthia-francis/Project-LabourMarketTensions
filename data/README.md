# 📂 `data/` — Données du projet

Ce dossier contient tous les fichiers de données nécessaires au projet, organisés en fonction des différentes étapes de traitement. Les données sont divisées en plusieurs sous-dossiers selon leur état, de la donnée brute à la version finale utilisée dans l'application intéractive.

## Structure des Dossiers

### Données Brutes :

Contient les fichiers de données brutes organisés par source. Pour les fichiers de petite taille, les données sont directement stockées ici. Pour les fichiers volumineux, des instructions sont fournies pour télécharger les bases de données nécessaires.

### Données Intermédiaires :

Contient les fichiers de données issues des scriptes. Ces fichiers ont subi un premier traitement et contiennent les résultats intermédiaires prêts pour une utilisation ultérieur.

### Données Formatées :

Contient les fichiers de données issues des scriptes. Ces fichiers ont subi un premier traitement et contiennent les résultats intermédiaires prêts pour une utilisation ultérieur.

### Données Finales :

Contient la base de données finale, prête à être utilisée dans la dernière étape du projet --\> développement de l'application.

### Tables de Liaison :

Contient des tables supplémentaires utilisées dans le traitement des données. Ces tables sont nécessaires pour effectuer certaines liaisons (communes à zone d'emploi, ROME à FAP...)

### Shapefiles :

Contient les shapefiles nécessaires à la création de la carte dans l'application intéractive.

------------------------------------------------------------------------

# 📂 `data/` — Project Data

This folder contains all the necessary data files for the project, organized across different stages of processing. The data is divided into several subfolders based on its current state, from raw data to the final version used in the interactive application.

## Folder Structure

### Raw Data:

Contains the raw data files organized by source. For smaller files, the data is directly stored here. For larger files, instructions are provided to download the required databases.

### Intermediate Data:

Contains the data files from the scripts. These files have undergone initial processing and contain the intermediate results ready for further use.

### Formatted Data:

Contains the data files from the scripts. These files have undergone initial processing and contain the intermediate results ready for further use.

### Final Data:

Contains the final database, ready to be used in the last step of the project --\> application development.

### Linking Tables:

Contains additional tables used in data processing. These tables are necessary for performing certain linkages (communes to employment zones, ROME to FAP...).

### Shapefiles:

Contains the shapefiles required to create the map in the interactive application.
